# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/

# --- Virtualenvs ---
.venv/
venv/
# Anchor to repo root: the bare `env/` pattern was a footgun — it also swallowed
# `mava/configs/jaxgcrl/env/` (the hydra env config dir) for weeks before it
# was caught. Only ignore an `env/` directory at the repo root.
/env/

# --- Local outputs / logs ---
wandb/
runs/
logs/
outputs/
multirun/
results/
*.log
checkpoints/

# --- Bundled archives ---
cppo.zip
*.tar.gz
*.zip


cppo-code/

# --- Dev artefacts that snuck into the work area ---
example.py
Screenshot*.png

# --- Secrets / local config ---
.env
.env.*
!.env.example

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# --- JAX cache ---
.jax_cache/
